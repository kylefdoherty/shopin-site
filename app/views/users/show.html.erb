<h2><%= @user.name %> | <%= link_to 'Edit', edit_user_path %></h2>
<h2><%= @user.email %></h2>
<h3>User type: <%= @user.vendor? ? 'Vendor' : 'Basic User' %></h3>

<% if !@user.products.empty?  %>
  <%= render "seller" %>
<% end %>
  
<%= render "buyer" %>